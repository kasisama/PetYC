# 更新日志

本文件记录 PetYC 各版本中面向用户、部署者和开发者的主要变更。当前尚未发布的内容会标记为“开发中”；正式发布时再补充发布日期。

## v0.1.3（2026-08-28）

### 玩家引导与日常体验

- 新增“今日待办”，根据领养、签到、宠物状态、活动和冒险进度给出当天优先事项；领养完成后会继续引导玩家进入地图探索。
- 宠物状态、宠物列表、图鉴、赛季和进化界面统一使用中文名称，不再向玩家暴露形态、区域和活动内部键。
- 喂养、送礼、学习、购买和制造等需要物品的操作会列出可用选择并提供快捷按钮；商店同时显示每日、每周剩余限购数量。
- 战斗界面新增双方名称、生命值、回合结果和已解锁技能按钮，技能支持使用中文名称触发。
- 地图和远征按解锁状态精简展示；未开放地图收起，探索按钮和提示统一使用区域中文名。
- 统一玩家可见货币名称，移除猜拳、钓鱼和交易文案中的“金币”旧称；抽签规则明确遗迹抽签券的获取区域。
- 新手改名费用由 1000 星砂调整为 120 星砂，治疗费用由 500 星砂调整为 80 星砂，并将治疗费用开放到后台基础设置。

### 冒险、成长与经济修复

- 探索战斗超时会正确结算受伤状态，治疗后清理受伤和残留行动状态，避免玩家长期卡在“正在行动”。
- 装备、蓝图和掉落奖励统一显示玩家可读名称，穿戴操作支持使用背包序号，不再显示 UUID 或内部模板键。
- 觉醒存在多个分支时必须由玩家明确选择；形态变化后立即按新形态刷新已解锁技能。
- 好感商店只扣除当前出战宠物的好感，购买记录和每日/每周个人限购在同一事务中校验与写入。
- 收紧活动并行容量、异常恢复和玩家错误提示，降低重复结算、错误扣款及技术错误直接暴露给玩家的风险。

### 运营后台与更新系统

- 玩家管理新增带审计记录和幂等键的货币发放、扣除接口，并拒绝未知货币或余额不足操作。
- 内容、配置和生态接口补充运行消费者、配置刷新及玩家可见字段，后台生产资源重新构建并嵌入可执行文件。
- 在线更新支持多个 HTTPS 清单与下载镜像；主源不可用或校验失败时自动尝试后备源。
- 更新辅助进程强化启动前失败恢复、旧版本重启和回滚路径，并扩充 Windows/Linux 发布检查。
- 发布流水线增加版本元数据、默认配置敏感字段、可执行文件版本、镜像清单和签名验证；GitHub 二进制会优先发布，不再受 Gitee 镜像阻塞；同时支持自动初始化空的 Gitee 更新仓库，并兼容“Release 不存在时返回 200/null”及附件响应缺少下载地址的情况。

### 测试

- 新增中文名称、今日待办、技能按钮、觉醒分支、治疗解卡、装备显示、抽签文案、新手费用、好感扣除和商店限购回归测试。
- 新增货币运营接口、更新镜像回退、校验失败恢复和旧版本重启测试。
- 发布前通过 Go 全仓测试、`go vet`、管理后台 61 个单元测试和生产构建。

## v0.1.2（2026-08-27）

### 在线更新与版本恢复

- 在系统设置中新增“程序更新”卡片，支持自动检查、手动刷新、更新说明、下载进度、校验状态和重启等待提示。
- 新增受管理员会话保护的更新检查、状态查询和安装接口；默认缓存稳定版检查结果 24 小时，并阻止并发安装任务。
- Windows amd64 便携版和 Linux amd64 独立二进制支持在线下载、自动替换与重启；开发构建、Docker、systemd、非 amd64、程序目录不可写等环境自动降级为手动下载。
- 更新清单使用 Ed25519 签名，下载产物同时校验 SHA-256、文件大小、HTTPS 地址、版本号和平台信息。
- 更新前自动备份旧程序、`pet_game.db` 及现有的 WAL/SHM 文件；新版本未在 60 秒内通过版本健康检查时，自动恢复旧程序和数据库并重新启动。
- 新增无鉴权的 `/healthz` 存活探针，仅返回服务状态和构建版本，供更新辅助进程与后台重启检测使用。
- 新增更新清单签名、验签命令及 Windows/Linux 更新辅助进程，并补充完整的密钥配置、发布产物和恢复说明。

### QQ 官方机器人

- 新增 QQ 官方自定义菜单与指令面板同步：从当前已启用指令生成单聊菜单，并创建或更新单聊、群聊、频道和频道私信四类托管面板。
- 后台“机器人平台状态”页新增“同步菜单与指令”操作；同步必须填写审计原因，并记录成功或失败结果。
- 菜单同步会遵守 QQ 官方的菜单项、子菜单、面板元素、名称和描述长度限制；未变化的托管内容会跳过更新，避免无效发布。
- 支持 QQ 官方群消息使用独立 Markdown 正文发送，并保留纯文本降级；频道消息同样按能力选择 Markdown 或纯文本。
- 群消息自动移除开头的机器人提及标记，统一命令路由兼容 `/命令` 写法，降低官方群聊与指令面板触发失败率。
- 修正 QQ 官方被动回复的 `msg_id`、`event_id`、`msg_seq` 和引用消息组合；Markdown 群回复不再因额外引用字段出现正文重复，图片回复也使用正确的来源引用。
- 重做消息去重键：同一群消息即使同时由“被提及消息”和“全量群消息”事件投递也只处理一次，按钮互动则按事件 ID 去重。
- 去重器在 QQ 网关重连之间复用；旧连接取消后会主动关闭 WebSocket，已接手的消息仍可在 20 秒有界上下文内完成处理和回复。
- QQ 运行配置未发生有效变化时不再重启网关；已停止的同配置运行时仍可正常重建。
- 重复消息日志不再输出完整消息内容或完整标识符，仅保留事件类型和脱敏标识，减少日志泄露风险。

### 菜单与内容运营

- 菜单配置新增独立 `Markdown` 字段，与必填纯文本回复、可选图片并存；旧数据库迁移时自动添加字段，不修改既有回复和图片。
- 内容工作台支持编辑、搜索和查看菜单 Markdown 源码，并明确展示场景当前使用 Markdown 还是纯文本。
- 配置方案导入导出会完整保留菜单 Markdown；旧配置缺少该字段时自动补为空值，继续使用纯文本回复。
- 官方默认菜单和现代菜单模板补齐结构化 Markdown，涵盖主菜单、今日状态、互动、家族、攻击、钓鱼、成长图鉴、营地小队、账号隐私和远征等场景。
- 动态生成的玩家菜单新增对应的 Markdown 排版；平台未开启 Markdown 能力或场景未配置 Markdown 时，继续发送原纯文本内容。

### 管理后台体验

- 平台状态改为共享的 5 秒轮询源，侧边栏与平台页实时复用同一份状态，避免重复请求和页面间状态不一致。
- 页面切到后台时暂停轮询，重新可见后立即刷新；临时请求失败时保留最后一次成功数据，并把状态显示为“未知”而不是误报“离线”。
- 鉴权失败后停止自动轮询，显式刷新后可恢复；组件卸载和请求竞态不会遗留计时器或让旧请求覆盖新订阅周期。
- 保存平台配置或发起 QQ 网关重连后立即刷新状态；操作已成功但刷新失败时给出独立警告，不再把成功操作误报为失败。
- 重新构建并更新嵌入 Go 可执行文件的后台静态资源。

### 运行与发布

- 服务支持由上下文触发的优雅关闭，并在退出时等待通知任务结束、关闭数据库连接，为自动更新替换程序和数据库文件提供稳定边界。
- 启动日志中的产品名称由“QQ-Pet SaaS”统一为“QQ-Pet”。
- GitHub Release 产物由 `qq-pet-saas_*` 统一更名为 `petyc_*`。
- 发布流水线新增更新清单生成、SHA-256 与文件大小写入、Ed25519 签名及发布前验签，Release 同时上传清单和签名文件。
- `github.com/google/uuid` 与 `golang.org/x/sys` 调整为直接依赖，并清理重复的旧版校验记录。

### 测试与文档

- 新增更新清单签名、版本比较、平台选择、下载校验、并发安装、备份恢复、自动回滚和辅助进程端到端测试。
- 扩充 QQ 官方客户端、事件映射、菜单面板、网关取消、跨重连去重、隐私日志、Markdown 组包和运行时重连测试。
- 扩充菜单数据库迁移、配置保存、配置方案导入导出、默认 Markdown、斜杠命令和纯文本降级测试。
- 扩充后台更新卡片、平台状态轮询、页面可见性、鉴权失败、竞态清理、侧边栏状态和平台操作测试。
- 补充自动更新系统运维文档，以及 v0.1.0 实施、验收、修复交接和菜单场景图片方案等项目过程文档。

### 兼容性说明

- 数据库迁移为向后兼容变更：现有菜单纯文本与图片不会被覆盖，新增 Markdown 默认为空。
- 未配置或未获 QQ Markdown 权限的机器人仍使用纯文本菜单，不影响既有命令玩法。
- 自动更新仅面向受支持的便携二进制部署；受进程管理器或容器管理的实例继续通过原部署方式升级。

## 早期版本

v0.1.1、v0.1.0 及更早版本的发布说明请查看 [GitHub Releases](https://github.com/kasisama/PetYC/releases)。
